# Sample Data

This directory contains deterministic local replay data for first-run demos and tests.

- `ohlcv/us-local-replay.json`: 130 daily OHLCV bars each for `AAPL`, `MSFT`, and `QQQ`.

The payload is ready for `POST /api/v1/market-data/replay-bars`.
